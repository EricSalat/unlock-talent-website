---
import AccordionItem from "./AccordionItem.astro";
---

<AccordionItem
    header="API"
    body="  Seamlessly embed background checks into your existing onboading experience."
    content="Learn More"
/>
<AccordionItem
    header="Partners"
    body="Seamlessly embed background checks into your existing onboading experience."
    content="Learn More"
    color="#000"
    backgroundColor="var(--rosaPrincipal)"
/>
<AccordionItem
    header="Checkr Dashboard"
    body="  Seamlessly embed background checks into your existing onboading experience."
    content="Learn More"
    color="#000"
    backgroundColor="var(--azulFuerte)"
/>
   

<script>
    document.addEventListener('DOMContentLoaded', function () {
    const accordionHeaders = document.querySelectorAll('.accordion-header');

    accordionHeaders.forEach(header => {
        header.addEventListener('click', function () {
            const accordionItem = this.parentElement;
            const isActive = accordionItem.classList.contains('active');

            // Cerrar todos los elementos
            document.querySelectorAll('.accordion-item').forEach(item => {
                item.classList.remove('active');
            });

            // Si el elemento no estaba activo, lo activamos
            if (!isActive) {
                accordionItem.classList.add('active');
            }
        });
    });
});

</script>